import { ChangDataDirective } from './chang-data.directive';

describe('ChangDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangDataDirective();
    expect(directive).toBeTruthy();
  });
});
